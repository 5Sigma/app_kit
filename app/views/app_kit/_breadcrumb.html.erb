<% if content_for :breadcrumb %>
    <div class="ap-breadcrumb">
        <ul>
            <li><%= link_to icon('dashboard'), app_kit.dashboard_path  %></li>
            <%= yield :breadcrumb %>
        </ul>
        <% if content_for :breadcrumb_tools %>
            <div class="tools">
                <%= yield :breadcrumb_tools %>
            </div>
        <% end %>
    </div>
<% end %>
